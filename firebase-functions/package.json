{
  "name": "firebase-functions",
  "description": "Funções para automação de páginas HTML casca a partir de JSON aprovado no Firestore",
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  }
  ,
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "start": "firebase emulators:start --only functions"
  },
  "engines": {
    "node": "18"
  }
}
